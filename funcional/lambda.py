# Função identidade
#>>>lambda x: x

# Soma de dois valores
#>>>lambda x, y: x + y

#>>>(lambda x, y: x + y)(1, 2)

#Identidade
identidade = lambda x: x
identidade('Python')
soma = lambda x, y: x+y
soma(1,2)